---
- name: upgrade packages
  update_cache: yes

- name: upgrade packages
  apt:
    name: "*"
    state: latest

- name: dist-upgrade
  apt:
    upgrade: dist

- name: install packages
  kg:
  - htop
  - python3
  - python3-pip